{
  "name": "nourbest-os",
  "version": "2.0.0",
  "private": true,
  "description": "Arabic-first universal PWA workspace and Linux live-image foundation",
  "scripts": {
    "test": "node --check app.js && node --check sw.js && node tests/smoke.mjs"
  },
  "engines": {"node": ">=18"},
  "license": "MIT"
}
