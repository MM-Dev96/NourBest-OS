{
  "name": "nourbest-os",
  "version": "3.0.0",
  "private": true,
  "description": "Arabic-first universal PWA workspace and Linux live-image foundation",
  "scripts": {
    "test": "node --check app.js && node --check sw.js && node --check storage-db.js && node --check plugin-api.js && node tests/smoke.mjs",
    "test:e2e": "playwright test",
    "build:vendor": "esbuild node_modules/qrcode/lib/browser.js --bundle --minify --format=iife --global-name=QRCode --outfile=vendor/qrcode.min.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "dependencies": {
    "qrcode": "1.5.4"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.10.2",
    "@playwright/test": "1.55.0",
    "esbuild": "0.25.6"
  }
}
